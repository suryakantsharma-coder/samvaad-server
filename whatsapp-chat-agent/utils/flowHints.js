/**
 * Shown on the first step of guided WhatsApp flows (booking, prescriptions, reschedule, etc.).
 */
const FLOW_EXIT_HINT =
  "*To leave this guided flow at any time, reply exit.*";

module.exports = {
  FLOW_EXIT_HINT,
};
